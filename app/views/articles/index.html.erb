<div>
  <h1 class="font-bold text-4xl">Rails Articles!</h1>

  <ul>
    <% @articles.each do |article| %>
      <li>
        <h2 class='font-semibold text-2xl'>
          <%= article.title %>
        </h2>
        <p>
          <%= article.body %>
        </p>
      </li>
    <% end %>
  </ul>
</div>
